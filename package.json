{
  "name": "minimaven",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "lint": "oxlint",
    "fmt": "oxfmt --write src/"
  },
  "devDependencies": {
    "oxfmt": "^0.40.0",
    "oxlint": "^0.16.12",
    "wrangler": "^4.74.0"
  }
}
